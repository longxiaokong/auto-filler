# 开发进度

最后更新：2026-05-25

## P0 — MVP ✅

| 状态 | 任务 | 说明 |
|------|------|------|
| ✅ | chrome.storage 封装 | `utils/storage.ts` — getProfile / setProfile / getApiConfig / import / export |
| ✅ | Options 设置页 | `entrypoints/options/` — 用户信息录入 + API 配置 + 身份切换 |
| ✅ | DOM 字段扫描 | `entrypoints/content.ts` — 扫描 input/select/textarea，提取 label/name/placeholder |
| ✅ | LLM 语义匹配 | `utils/matcher.ts` — OpenAI 兼容 API，prompt 构建，JSON 解析 |
| ✅ | Background 编排 | `entrypoints/background.ts` — 扫描调度、LLM 匹配、消息路由、降级提示 |
| ✅ | 字段填充 | `entrypoints/content.ts` — 支持 input/select/textarea/radio/checkbox，兼容 React |
| ✅ | Popup UI | `entrypoints/popup/` — idle → scanning → result 四态流转，蓝白配色 |

## P1

| 状态 | 任务 | 说明 |
|------|------|------|
| ⬜ | JSON 导入/导出 | Options 页导出/导入 JSON 备份（storage.ts 已有 exportConfig/importConfig） |
| ⬜ | Popup 模板切换 | 顶部 Badge → 微型 select 下拉，直接切换 profileType |
| ⬜ | 从页面反填到 Profile | 扫描后若页面已有值，提供「保存到我的信息」按钮写回 storage |

## P2

| 状态 | 任务 | 说明 |
|------|------|------|
| ⬜ | 匹配过程加载动画 | Popup scanning 态的 spinner 已有基础，可进一步美化 |
| ⬜ | 匹配历史记录 | 记录每次匹配的页面 URL、字段映射、时间 |
| ⬜ | 云盘同步 | WebDAV / 网盘 API，上传下载用户配置 |
| ⬜ | 图片上传 + 拖拽排序 | 设置页管理证件照等图片 |
| ⬜ | PDF 拼装 | Popup 底部跳转链接，新 Tab 打开 options.html#pdf |
